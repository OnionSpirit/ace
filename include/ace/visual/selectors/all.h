#ifndef ACE_VISUAL_SELECTORS_ALL_H
#define ACE_VISUAL_SELECTORS_ALL_H

namespace ace::visual::selectors {

    // NOTE: Marks graph to await all branches and doesn't ignore branch fails
    struct all {};

}

#endif //ACE_VISUAL_SELECTORS_ALL_H
